<%= form_for @salle, html: { multipart: true } do |f| %>
  <% if @salle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salle.errors.count, "error") %> prohibited this salle from being saved:</h2>

      <ul>
      <% @salle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%#= form_tag 'salles', :multipart => true do %>
   <%#<label for="file">Upload text File</label> %>
    <%= f.label :file %><br>
   <%= f.file_field :file %>
   
   <%#= submit_tag %>
<%# end %> 


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
